syntax = "proto3";

package myrtlespeech.protos;

import "myrtlespeech/protos/encoder.proto";
import "myrtlespeech/protos/decoder.proto";

// Configuration for Encoder-Decoder models.
message EncoderDecoder {
  // Output alphabet.
  string alphabet = 1;

  // Encoder config.
  Encoder encoder = 2;

  // Decoder config.
  Decoder decoder = 3;
}
