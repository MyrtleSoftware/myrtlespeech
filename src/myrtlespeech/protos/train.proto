syntax = "proto3";

package myrtlespeech.protos;

import "myrtlespeech/protos/preprocessor.proto";

// Configuration for training.
message TrainConfig {
  // Preprocessing/data augmentation steps.
  repeated PreprocessingStep preprocessing_step = 1;
}
