syntax = "proto3";

package myrtlespeech.protos;

import "myrtlespeech/protos/fully_connected.proto";

// Configuration for the Decoder part of an Encoder-Decoder model.
message Decoder {
  oneof supported_decoders {
    FullyConnected fully_connected = 1;
  }
}
