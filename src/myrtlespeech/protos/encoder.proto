syntax = "proto3";

package myrtlespeech.protos;

import "myrtlespeech/protos/cnn_rnn_encoder.proto";


// Configuration for the encoder part of an encoder-decoder model.
message Encoder {
  oneof supported_encoders {
    CNNRNNEncoder cnn_rnn_encoder = 1;
  }
}
